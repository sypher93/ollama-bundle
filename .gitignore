# Installation logs
installation.log
*.log

# SSL certificates (never commit private keys!)
ssl/
*.key
*.crt
*.pem

# Docker Compose override files
docker-compose.override.yml

# Environment files
.env
.env.local
.env.*.local

# Nginx configuration with sensitive data
# (the template should be committed, but not generated ones with real IPs/domains)
conf.d/open-webui.conf

# Backup files
*.bak
*.backup
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.git

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~
to-do*

# Temporary files
tmp/
temp/
*.tmp

# Docker volumes data (should be in volumes, but just in case)
data/
volumes/